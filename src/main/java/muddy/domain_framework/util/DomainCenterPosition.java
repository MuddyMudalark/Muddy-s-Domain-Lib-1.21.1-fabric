package muddy.domain_framework.util;

public interface DomainCenterPosition {
    Domain domain$getDomain();
    boolean domain$shouldRenderInternalDomain();
    void domain$setDomain(Domain domain);
}
